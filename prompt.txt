Ты работаешь в проекте monthly_report. Нужно за один проход довести проект до полностью рабочего состояния "из коробки" на Windows для сверки платежей между двумя XLSX.

Сделай следующее:

1) Основной скрипт compare_payments.py
- Реализуй сравнение платежей двух Excel-файлов по персональному номеру.
- Файл 1: начиная с 9 строки, номер компании извлекать из текста вида [123] (по спецификации колонка D, но поддержи fallback на C), сумма в AF.
- Файл 2: начиная с 5 строки, номер в колонке A формата "*,000" (брать только часть до запятой), сумма в F, название компании брать из C с fallback на B.
- Сопоставление строго по персональному номеру.
- Учитывать случаи, когда компания есть только в одном файле.
- При дубликатах ID внутри одного файла агрегировать суммы.
- Сформировать итоговый Excel-отчет с листами:
  - comparison (детализация)
  - summary (сводка)
- В comparison должны быть колонки:
  - company_id
  - company_name_<имя_файла_1>
  - company_name_<имя_файла_2>
  - amount__<имя_файла_1>
  - amount__<имя_файла_2>
  - delta
  - status
- Статусы:
  - MATCH
  - MISMATCH
  - only_file_<имя_файла_1>
  - only_file_<имя_файла_2>
- Форматирование итогового Excel:
  - MATCH: светло-зеленая заливка всей строки
  - остальные статусы: светло-красная заливка всей строки
  - столбцы D,E,F: формат числа #,##0.00
  - сортировка данных в comparison по столбцу A по возрастанию
  - заголовки всегда в первой строке
  - включить автофильтр
  - автоширина столбцов по содержимому
- Подавить несущественное warning openpyxl про print area в консоли.
- После формирования отчета автоматически открыть файл через ассоциированное приложение Windows.

2) CLI + GUI
- Оставь поддержку CLI параметров:
  - --file1 (по умолчанию work/OC.xlsx)
  - --file2 (по умолчанию work/1C.xlsx)
  - --out (по умолчанию reports/payments_comparison.xlsx)
  - --gui
- Реализуй GUI (tkinter) в том же скрипте:
  - Поле "Укажите отчет OC"
  - Поле "Укажите отчет 1С"
  - Поле выбора выходного файла
  - Кнопка "Сформировать отчет"
  - Проверки существования входных файлов и понятные сообщения об ошибках
  - Сообщение об успехе с краткой статистикой

3) Портативный запуск на новом ПК
- Добавь requirements.txt с pandas и openpyxl.
- Добавь setup.ps1 (ASCII, без проблем кодировки), который:
  - находит Python (py -3 или python)
  - создает .venv (если нет)
  - обновляет pip
  - ставит зависимости из requirements.txt
- Добавь лаунчеры:
  - "Сверка банка.bat" (проверяет .venv, запускает GUI)
  - "Сверка банка.vbs" (скрытый запуск GUI без окна терминала)

4) Документация
- Обнови README.md:
  - что делает проект
  - как запускать CLI и GUI
  - как подготовить окружение одной командой
  - что делать при ошибке ".venv is not found"
- Создай/обнови README_PORTABLE.md:
  - перенос проекта на флешке
  - подготовка на новом ПК одной командой
  - запуск двойным кликом через "Сверка банка.vbs"
  - fallback запуск через bat/CLI

5) Проверка
- Запусти проверки минимум:
  - python compare_payments.py --help
  - python compare_payments.py --out reports/payments_comparison_test.xlsx
- Убедись, что отчет создается.

Ограничения по стилю:
- Не ломай существующую рабочую логику сравнения.
- Делай изменения минимально инвазивно.
- Пиши код и скрипты так, чтобы не было проблем с кодировками в cmd/powershell.

В конце выведи:
- список измененных файлов
- краткий итог, как запускать пользователю (setup + двойной клик).
